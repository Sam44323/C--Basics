#include <iostream>

using std::cout; // using the declarative approach (will work for entire program)

int main()
{
  // if declared here then going to work for this block only

  cout << "Using the declarative approach\n";
  return 0;
}